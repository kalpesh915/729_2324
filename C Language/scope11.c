/**
    enumeration data type
    user defined data type
*/

#include<stdio.h>

enum boolean {false , true};

void main(){
    enum boolean status;

    status = false;
    printf("\n status is %d", status);
}
